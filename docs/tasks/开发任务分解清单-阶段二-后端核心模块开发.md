# AIForum 课题组学术交流平台

## 开发任务分解清单 - 阶段二：后端核心模块开发

| 文档信息 | 内容 |
|----------|------|
| 文档版本 | V1.0 |
| 创建日期 | 2026-03-04 |
| 编写人员 | 技术负责人 |

---

## 任务清单概览

| 任务编号 | 任务名称 | 预估工时 | 优先级 |
|----------|----------|----------|--------|
| T2-001 | 数据库模型设计 | 1天 | P0 |
| T2-002 | 用户认证模块 | 1.5天 | P0 |
| T2-003 | 用户管理模块 | 1天 | P0 |
| T2-004 | 论文管理模块 | 2天 | P0 |
| T2-005 | 经验贴模块 | 1.5天 | P0 |
| T2-006 | 下载中心模块 | 1天 | P1 |
| T2-007 | 论坛功能模块 | 1.5天 | P0 |
| T2-008 | 搜索功能模块 | 1天 | P0 |
| T2-009 | 文件存储模块 | 0.5天 | P0 |

---

## 任务详情

### T2-001: 数据库模型设计（Tortoise ORM）

| 属性 | 内容 |
|------|------|
| **任务编号** | T2-001 |
| **任务名称** | 数据库模型设计（Tortoise ORM） |
| **预估工时** | 1天 |
| **优先级** | P0 |

**任务描述**：
使用Tortoise ORM设计所有数据库模型，创建异步数据库连接，配置自动表结构生成。

**具体步骤**：
1. 配置Tortoise ORM连接
2. 创建用户模型 (`User`)
3. 创建论文模型 (`Paper`)
4. 创建经验贴模型 (`Post`)
5. 创建评论模型 (`Comment`)
6. 创建点赞和收藏模型 (`Like`, `Favorite`)
7. 创建下载中心模型 (`Download`)
8. 创建AI对话模型 (`AIConversation`, `AIMessage`)
9. 配置数据库初始化脚本
10. 执行自动表结构生成

**核心模型代码** (`backend/app/models/user.py`)：
```python
from tortoise import fields, models
from tortoise.enums import EnumType

class User(models.Model):
    """用户模型"""
    id = fields.IntField(pk=True, auto_increment=True)
    name = fields.CharField(max_length=50, null=False)
    student_id = fields.CharField(max_length=20, unique=True, null=False)
    grade = fields.CharField(max_length=10, null=True)
    email = fields.CharField(max_length=100, null=True)
    phone = fields.CharField(max_length=20, null=True)
    research_direction = fields.CharField(max_length=255, null=True)
    wechat = fields.CharField(max_length=50, null=True)
    password_hash = fields.CharField(max_length=255, null=False)
    role = fields.CharEnumField(
        enum_type=EnumType(('student', 'student_admin', 'teacher')),
        default='student'
    )
    is_admin = fields.BooleanField(default=False)
    created_at = fields.DatetimeField(auto_now_add=True)
    updated_at = fields.DatetimeField(auto_now=True)
    
    class Meta:
        table = "users"
```

**数据库初始化代码** (`backend/app/core/database.py`)：
```python
from tortoise import Tortoise, run_async

TORTOISE_ORM = {
    "connections": {
        "default": "mysql://root:password@localhost:3306/aiforum"
    },
    "apps": {
        "models": {
            "models": ["app.models.user", "app.models.paper", ...],
            "default_connection": "default",
        },
    },
}

async def init_db():
    """初始化数据库"""
    await Tortoise.init(config=TORTOISE_ORM)
    await Tortoise.generate_schemas()
```

**依赖关系**：
- 依赖：T1-002 后端基础环境搭建
- 依赖：T1-004 数据库环境搭建

**验收条件**：
- [ ] 所有Tortoise ORM模型创建完成
- [ ] 数据库连接配置完成
- [ ] 自动表结构生成成功
- [ ] 数据库表创建成功
- [ ] 模型关系正确
- [ ] 异步操作正常

---

### T2-002: 用户认证模块

| 属性 | 内容 |
|------|------|
| **任务编号** | T2-002 |
| **任务名称** | 用户认证模块 |
| **预估工时** | 1.5天 |
| **优先级** | P0 |

**任务描述**：
实现用户注册、登录、密码管理等认证功能，集成JWT Token认证。

**具体步骤**：
1. 实现密码加密和验证
2. 实现JWT Token生成和验证
3. 实现用户注册API
4. 实现用户登录API
5. 实现密码修改API
6. 实现登出API
7. 实现当前用户获取API
8. 配置OAuth2认证依赖
9. 编写单元测试

**核心代码** (`backend/app/api/auth.py`)：
```python
from fastapi import APIRouter, HTTPException, status
from fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm
from app.core.security import verify_password, get_password_hash, create_access_token
from app.models.user import User
from app.schemas.user import UserCreate, UserLogin, UserResponse

router = APIRouter(prefix="/auth", tags=["认证"])

@router.post("/register", response_model=UserResponse)
async def register(user_data: UserCreate):
    """用户注册"""
    # 检查学号是否已存在
    existing_user = await User.filter(student_id=user_data.student_id).first()
    if existing_user:
        raise HTTPException(status_code=400, detail="学号已存在")
    
    # 创建用户
    user = await User.create(
        name=user_data.name,
        student_id=user_data.student_id,
        grade=user_data.grade,
        password_hash=get_password_hash(user_data.password)
    )
    return user

@router.post("/login")
async def login(form_data: OAuth2PasswordRequestForm = Depends()):
    """用户登录"""
    # 查找用户（支持学号/姓名登录）
    user = await User.filter(
        (User.student_id == form_data.username) | 
        (User.name == form_data.username)
    ).first()
    
    if not user or not verify_password(form_data.password, user.password_hash):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="用户名或密码错误"
        )
    
    # 生成Token
    access_token = create_access_token(data={"sub": str(user.id)})
    return {"access_token": access_token, "token_type": "bearer"}
```

**依赖关系**：
- 依赖：T2-001 数据库模型设计

**验收条件**：
- [ ] 注册API正常工作
- [ ] 登录API正常工作
- [ ] JWT Token生成和验证正常
- [ ] 密码加密存储
- [ ] 单元测试通过

---

### T2-003: 用户管理模块

| 属性 | 内容 |
|------|------|
| **任务编号** | T2-003 |
| **任务名称** | 用户管理模块 |
| **预估工时** | 1天 |
| **优先级** | P0 |

**任务描述**：
实现用户信息管理、权限管理等功能。

**具体步骤**：
1. 实现获取当前用户信息API
2. 实现更新用户信息API
3. 实现用户列表API（管理员）
4. 实现用户详情API（管理员）
5. 实现修改用户权限API（管理员）
6. 实现删除用户API（管理员）
7. 实现批量添加教师用户功能
8. 编写单元测试

**核心代码** (`backend/app/api/users.py`)：
```python
from fastapi import APIRouter, Depends, HTTPException
from typing import List
from app.core.security import get_current_user, get_current_admin
from app.models.user import User
from app.schemas.user import UserResponse, UserUpdate

router = APIRouter(prefix="/users", tags=["用户"])

@router.get("/me", response_model=UserResponse)
async def get_me(current_user: User = Depends(get_current_user)):
    """获取当前用户信息"""
    return current_user

@router.put("/me", response_model=UserResponse)
async def update_me(
    user_data: UserUpdate,
    current_user: User = Depends(get_current_user)
):
    """更新当前用户信息"""
    for field, value in user_data.dict(exclude_unset=True).items():
        setattr(current_user, field, value)
    await current_user.save()
    return current_user

@router.get("/", response_model=List[UserResponse])
async def list_users(
    skip: int = 0,
    limit: int = 100,
    current_user: User = Depends(get_current_admin)
):
    """获取用户列表（管理员）"""
    users = await User.all().offset(skip).limit(limit)
    return users

@router.put("/{user_id}/role")
async def update_user_role(
    user_id: int,
    role: str,
    is_admin: bool,
    current_user: User = Depends(get_current_admin)
):
    """修改用户权限（管理员）"""
    user = await User.get_or_none(id=user_id)
    if not user:
        raise HTTPException(status_code=404, detail="用户不存在")
    
    user.role = role
    user.is_admin = is_admin
    await user.save()
    return {"message": "权限更新成功"}
```

**依赖关系**：
- 依赖：T2-002 用户认证模块

**验收条件**：
- [ ] 用户信息获取和更新正常
- [ ] 管理员用户管理功能正常
- [ ] 权限控制正确
- [ ] 单元测试通过

---

### T2-004: 论文管理模块

| 属性 | 内容 |
|------|------|
| **任务编号** | T2-004 |
| **任务名称** | 论文管理模块 |
| **预估工时** | 2天 |
| **优先级** | P0 |

**任务描述**：
实现论文上传、解析、展示、下载等完整功能。

**具体步骤**：
1. 实现论文上传API
2. 实现论文解析功能（PDF/DOCX）
3. 实现OCR解析功能（Free OCR API）
4. 实现论文列表API
5. 实现论文详情API
6. 实现论文修改API
7. 实现论文删除API
8. 实现论文下载API
9. 实现论文搜索功能
10. 编写单元测试

**核心代码** (`backend/app/services/paper_parser.py`)：
```python
import os
import re
import PyPDF2
from docx import Document
import requests

class PaperParser:
    def parse(self, file_path: str) -> dict:
        """解析论文文件"""
        # 提取文本
        text = self.extract_text(file_path)
        
        # 如果文本提取失败，尝试OCR
        if not text:
            text = self.ocr_with_free_api(file_path)
        
        # 提取元数据
        metadata = {
            'title': self.extract_title(text),
            'authors': self.extract_authors(text),
            'abstract': self.extract_abstract(text),
            'keywords': self.extract_keywords(text),
            'doi': self.extract_doi(text),
            'paper_type': self.determine_paper_type(text)
        }
        
        return metadata
    
    def extract_text(self, file_path: str) -> str:
        """提取文本内容"""
        if file_path.endswith('.pdf'):
            return self.extract_pdf_text(file_path)
        elif file_path.endswith('.docx'):
            return self.extract_docx_text(file_path)
        return ""
    
    def extract_pdf_text(self, file_path: str) -> str:
        """提取PDF文本"""
        try:
            with open(file_path, 'rb') as f:
                reader = PyPDF2.PdfReader(f)
                text = ""
                for page in reader.pages:
                    text += page.extract_text() + "\n"
                return text
        except Exception as e:
            print(f"PDF提取错误: {e}")
            return ""
    
    def ocr_with_free_api(self, file_path: str) -> str:
        """使用Free OCR API进行OCR"""
        api_key = os.getenv('OCR_API_KEY')
        url = "https://api.ocr.space/parse/image"
        
        try:
            with open(file_path, 'rb') as f:
                files = {'file': f}
                data = {
                    'apikey': api_key,
                    'language': 'eng',
                    'isOverlayRequired': False
                }
                response = requests.post(url, files=files, data=data)
                result = response.json()
                
                if result.get('IsErroredOnProcessing'):
                    return ""
                
                parsed_results = result.get('ParsedResults', [])
                if parsed_results:
                    return parsed_results[0].get('ParsedText', "")
                return ""
        except Exception as e:
            print(f"OCR错误: {e}")
            return ""
    
    def extract_title(self, text: str) -> str:
        """提取标题"""
        lines = text.strip().split('\n')
        for line in lines[:10]:  # 在前10行中查找
            line = line.strip()
            if len(line) > 10 and len(line) < 200:
                return line
        return ""
    
    def extract_doi(self, text: str) -> str:
        """提取DOI"""
        doi_pattern = r'10\.\d{4,}/[^\s]+'
        match = re.search(doi_pattern, text)
        return match.group(0) if match else ""
    
    def determine_paper_type(self, text: str) -> str:
        """判断论文类型"""
        if 'DOI' in text or 'doi' in text:
            return 'journal'
        elif '学位论文' in text or 'thesis' in text.lower():
            return 'thesis'
        return 'journal'
```

**依赖关系**：
- 依赖：T2-001 数据库模型设计
- 依赖：T2-002 用户认证模块
- 依赖：T2-009 文件存储模块

**验收条件**：
- [ ] 论文上传功能正常
- [ ] 论文解析功能正常（PDF/DOCX）
- [ ] OCR解析功能正常
- [ ] 论文CRUD功能正常
- [ ] 论文下载功能正常
- [ ] 单元测试通过

---

### T2-005: 经验贴模块

| 属性 | 内容 |
|------|------|
| **任务编号** | T2-005 |
| **任务名称** | 经验贴模块 |
| **预估工时** | 1.5天 |
| **优先级** | P0 |

**任务描述**：
实现经验贴的发布、编辑、展示、置顶等功能。

**具体步骤**：
1. 实现经验贴发布API
2. 实现经验贴列表API（支持分类和置顶）
3. 实现经验贴详情API
4. 实现经验贴修改API
5. 实现经验贴删除API
6. 实现经验贴置顶API（管理员）
7. 实现草稿保存功能
8. 实现附件管理功能
9. 实现Markdown上传转换功能
10. 编写单元测试

**核心代码** (`backend/app/api/posts.py`)：
```python
from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.orm import Session
from typing import List, Optional
from app.core.database import get_db
from app.core.security import get_current_user, get_current_admin
from app.models.post import Post
from app.schemas.post import PostCreate, PostUpdate, PostResponse

router = APIRouter(prefix="/posts", tags=["经验贴"])

@router.get("/", response_model=List[PostResponse])
def list_posts(
    category: Optional[str] = None,
    skip: int = 0,
    limit: int = 20,
    db: Session = Depends(get_db)
):
    """获取经验贴列表"""
    query = db.query(Post)
    
    if category:
        query = query.filter(Post.category == category)
    
    # 置顶帖排在前面
    posts = query.order_by(
        Post.is_pinned.desc(),
        Post.created_at.desc()
    ).offset(skip).limit(limit).all()
    
    return posts

@router.post("/", response_model=PostResponse)
def create_post(
    post_data: PostCreate,
    current_user = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """发布经验贴"""
    post = Post(
        title=post_data.title,
        content=post_data.content,
        category=post_data.category,
        author_id=current_user.id
    )
    db.add(post)
    db.commit()
    db.refresh(post)
    return post

@router.put("/{post_id}/pin")
def pin_post(
    post_id: int,
    is_pinned: bool,
    current_user = Depends(get_current_admin),
    db: Session = Depends(get_db)
):
    """置顶/取消置顶经验贴（管理员）"""
    post = db.query(Post).filter(Post.id == post_id).first()
    if not post:
        raise HTTPException(status_code=404, detail="经验贴不存在")
    
    post.is_pinned = is_pinned
    db.commit()
    return {"message": "置顶状态更新成功"}
```

**依赖关系**：
- 依赖：T2-001 数据库模型设计
- 依赖：T2-002 用户认证模块

**验收条件**：
- [ ] 经验贴发布功能正常
- [ ] 经验贴列表支持分类和置顶排序
- [ ] 经验贴CRUD功能正常
- [ ] 置顶功能正常（管理员）
- [ ] 草稿功能正常
- [ ] 单元测试通过

---

### T2-006: 下载中心模块

| 属性 | 内容 |
|------|------|
| **任务编号** | T2-006 |
| **任务名称** | 下载中心模块 |
| **预估工时** | 1天 |
| **优先级** | P1 |

**任务描述**：
实现下载中心资源的上传、展示、下载功能。

**具体步骤**：
1. 实现资源上传API（仅管理员）
2. 实现资源列表API
3. 实现资源详情API
4. 实现资源修改API（仅管理员）
5. 实现资源删除API（仅管理员）
6. 实现资源下载API
7. 实现下载次数统计
8. 编写单元测试

**依赖关系**：
- 依赖：T2-001 数据库模型设计
- 依赖：T2-002 用户认证模块
- 依赖：T2-009 文件存储模块

**验收条件**：
- [ ] 资源上传功能正常（仅管理员）
- [ ] 资源CRUD功能正常
- [ ] 资源下载功能正常
- [ ] 下载次数统计正确
- [ ] 单元测试通过

---

### T2-007: 论坛功能模块

| 属性 | 内容 |
|------|------|
| **任务编号** | T2-007 |
| **任务名称** | 论坛功能模块 |
| **预估工时** | 1.5天 |
| **优先级** | P0 |

**任务描述**：
实现评论、点赞、收藏等论坛功能。

**具体步骤**：
1. 实现发表评论API
2. 实现评论列表API
3. 实现评论回复功能
4. 实现点赞API
5. 实现取消点赞API
6. 实现收藏API
7. 实现取消收藏API
8. 实现收藏列表API
9. 实现点赞数、收藏数统计
10. 编写单元测试

**核心代码** (`backend/app/api/forum.py`)：
```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from typing import List
from app.core.database import get_db
from app.core.security import get_current_user
from app.models.comment import Comment
from app.models.like import Like
from app.models.favorite import Favorite
from app.schemas.forum import CommentCreate, CommentResponse

router = APIRouter(prefix="/forum", tags=["论坛"])

@router.post("/comments")
def create_comment(
    comment_data: CommentCreate,
    current_user = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """发表评论"""
    comment = Comment(
        post_id=comment_data.post_id,
        user_id=current_user.id,
        content=comment_data.content,
        parent_id=comment_data.parent_id
    )
    db.add(comment)
    
    # 更新评论数
    post = db.query(Post).filter(Post.id == comment_data.post_id).first()
    post.comment_count += 1
    
    db.commit()
    db.refresh(comment)
    return comment

@router.post("/likes")
def toggle_like(
    target_type: str,
    target_id: int,
    current_user = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """点赞/取消点赞"""
    existing_like = db.query(Like).filter(
        Like.user_id == current_user.id,
        Like.target_type == target_type,
        Like.target_id == target_id
    ).first()
    
    if existing_like:
        # 取消点赞
        db.delete(existing_like)
        # 更新点赞数
        if target_type == 'post':
            post = db.query(Post).filter(Post.id == target_id).first()
            post.like_count -= 1
        db.commit()
        return {"message": "取消点赞成功"}
    else:
        # 添加点赞
        like = Like(
            user_id=current_user.id,
            target_type=target_type,
            target_id=target_id
        )
        db.add(like)
        # 更新点赞数
        if target_type == 'post':
            post = db.query(Post).filter(Post.id == target_id).first()
            post.like_count += 1
        db.commit()
        return {"message": "点赞成功"}

@router.post("/favorites")
def toggle_favorite(
    target_type: str,
    target_id: int,
    current_user = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """收藏/取消收藏"""
    existing_favorite = db.query(Favorite).filter(
        Favorite.user_id == current_user.id,
        Favorite.target_type == target_type,
        Favorite.target_id == target_id
    ).first()
    
    if existing_favorite:
        # 取消收藏
        db.delete(existing_favorite)
        # 更新收藏数
        if target_type == 'paper':
            paper = db.query(Paper).filter(Paper.id == target_id).first()
            paper.favorite_count -= 1
        db.commit()
        return {"message": "取消收藏成功"}
    else:
        # 添加收藏
        favorite = Favorite(
            user_id=current_user.id,
            target_type=target_type,
            target_id=target_id
        )
        db.add(favorite)
        # 更新收藏数
        if target_type == 'paper':
            paper = db.query(Paper).filter(Paper.id == target_id).first()
            paper.favorite_count += 1
        db.commit()
        return {"message": "收藏成功"}
```

**依赖关系**：
- 依赖：T2-001 数据库模型设计
- 依赖：T2-002 用户认证模块

**验收条件**：
- [ ] 评论功能正常
- [ ] 点赞功能正常
- [ ] 收藏功能正常
- [ ] 统计数据更新正确
- [ ] 单元测试通过

---

### T2-008: 搜索功能模块

| 属性 | 内容 |
|------|------|
| **任务编号** | T2-008 |
| **任务名称** | 搜索功能模块 |
| **预估工时** | 1天 |
| **优先级** | P0 |

**任务描述**：
实现全文搜索功能，支持论文、经验贴、下载中心的搜索。

**具体步骤**：
1. 实现论文搜索功能
2. 实现经验贴搜索功能
3. 实现下载中心搜索功能
4. 实现综合搜索功能
5. 实现搜索结果高亮
6. 实现搜索结果排序（相关度+时间）
7. 编写单元测试

**核心代码** (`backend/app/api/search.py`)：
```python
from fastapi import APIRouter, Depends, Query
from sqlalchemy.orm import Session
from sqlalchemy import or_, func
from typing import List, Optional
from app.core.database import get_db
from app.models.paper import Paper
from app.models.post import Post
from app.models.download import Download
from app.schemas.search import SearchResult

router = APIRouter(prefix="/search", tags=["搜索"])

@router.get("/papers")
def search_papers(
    keyword: str,
    page: int = 1,
    page_size: int = 20,
    db: Session = Depends(get_db)
):
    """搜索论文"""
    search_query = f"%{keyword}%"
    
    papers = db.query(Paper).filter(
        or_(
            Paper.title.like(search_query),
            Paper.authors.like(search_query),
            Paper.keywords.like(search_query),
            Paper.abstract.like(search_query),
            Paper.doi.like(search_query)
        )
    ).order_by(
        # 相关度排序
        func.length(Paper.title) - func.length(func.replace(Paper.title, keyword, '')),
        Paper.upload_time.desc()
    ).offset((page-1)*page_size).limit(page_size).all()
    
    total = db.query(Paper).filter(
        or_(
            Paper.title.like(search_query),
            Paper.authors.like(search_query),
            Paper.keywords.like(search_query),
            Paper.abstract.like(search_query),
            Paper.doi.like(search_query)
        )
    ).count()
    
    return {
        "total": total,
        "page": page,
        "page_size": page_size,
        "items": papers
    }

@router.get("/posts")
def search_posts(
    keyword: str,
    page: int = 1,
    page_size: int = 20,
    db: Session = Depends(get_db)
):
    """搜索经验贴"""
    search_query = f"%{keyword}%"
    
    posts = db.query(Post).filter(
        or_(
            Post.title.like(search_query),
            Post.content.like(search_query)
        )
    ).order_by(
        Post.created_at.desc()
    ).offset((page-1)*page_size).limit(page_size).all()
    
    total = db.query(Post).filter(
        or_(
            Post.title.like(search_query),
            Post.content.like(search_query)
        )
    ).count()
    
    return {
        "total": total,
        "page": page,
        "page_size": page_size,
        "items": posts
    }
```

**依赖关系**：
- 依赖：T2-001 数据库模型设计

**验收条件**：
- [ ] 论文搜索功能正常
- [ ] 经验贴搜索功能正常
- [ ] 下载中心搜索功能正常
- [ ] 搜索结果排序正确
- [ ] 单元测试通过

---

### T2-009: 文件存储模块

| 属性 | 内容 |
|------|------|
| **任务编号** | T2-009 |
| **任务名称** | 文件存储模块 |
| **预估工时** | 0.5天 |
| **优先级** | P0 |

**任务描述**：
实现文件上传、存储、下载功能。

**具体步骤**：
1. 实现文件上传功能
2. 实现文件存储路径管理
3. 实现文件下载功能
4. 实现文件类型验证
5. 实现文件大小限制
6. 编写单元测试

**核心代码** (`backend/app/services/files.py`)：
```python
import os
import uuid
from datetime import datetime
from fastapi import UploadFile

class FileService:
    def __init__(self, upload_dir: str = "uploads"):
        self.upload_dir = upload_dir
    
    def save_file(self, file: UploadFile, file_type: str) -> str:
        """保存文件"""
        # 生成唯一文件名
        timestamp = datetime.now().strftime('%Y%m%d%H%M%S')
        ext = os.path.splitext(file.filename)[1]
        file_name = f"{timestamp}_{uuid.uuid4().hex[:8]}{ext}"
        
        # 确定存储路径
        if file_type == 'paper':
            path = f"{self.upload_dir}/papers/{datetime.now().strftime('%Y/%m')}"
        elif file_type == 'attachment':
            path = f"{self.upload_dir}/attachments/{datetime.now().strftime('%Y/%m')}"
        else:
            path = f"{self.upload_dir}/temp/{timestamp}"
        
        # 确保目录存在
        os.makedirs(path, exist_ok=True)
        
        # 保存文件
        file_path = os.path.join(path, file_name)
        with open(file_path, 'wb') as f:
            content = file.file.read()
            f.write(content)
        
        return file_path
    
    def validate_file(self, file: UploadFile, max_size: int = 50*1024*1024) -> bool:
        """验证文件"""
        # 检查文件大小
        file.file.seek(0, 2)
        size = file.file.tell()
        file.file.seek(0)
        
        if size > max_size:
            return False
        
        # 检查文件类型
        allowed_extensions = ['.pdf', '.doc', '.docx', '.md', '.txt']
        ext = os.path.splitext(file.filename)[1].lower()
        if ext not in allowed_extensions:
            return False
        
        return True
```

**依赖关系**：
- 依赖：T1-002 后端基础环境搭建

**验收条件**：
- [ ] 文件上传功能正常
- [ ] 文件存储路径正确
- [ ] 文件下载功能正常
- [ ] 文件验证功能正常
- [ ] 单元测试通过

---

## 阶段二总结

| 任务 | 状态 | 工时 |
|------|------|------|
| T2-001 数据库模型设计 | 待开始 | 1天 |
| T2-002 用户认证模块 | 待开始 | 1.5天 |
| T2-003 用户管理模块 | 待开始 | 1天 |
| T2-004 论文管理模块 | 待开始 | 2天 |
| T2-005 经验贴模块 | 待开始 | 1.5天 |
| T2-006 下载中心模块 | 待开始 | 1天 |
| T2-007 论坛功能模块 | 待开始 | 1.5天 |
| T2-008 搜索功能模块 | 待开始 | 1天 |
| T2-009 文件存储模块 | 待开始 | 0.5天 |
| **总计** | - | **11天** |

---

**下一步**：进入阶段三：前端核心模块开发
