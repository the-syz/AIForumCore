# AIForum 课题组学术交流平台

## 开发任务分解清单 - 阶段三：前端核心模块开发

| 文档信息 | 内容       |
| -------- | ---------- |
| 文档版本 | V1.0       |
| 创建日期 | 2026-03-04 |
| 编写人员 | 技术负责人 |

---

## 任务清单概览

| 任务编号 | 任务名称         | 预估工时 | 优先级 |
| -------- | ---------------- | -------- | ------ |
| T3-001   | 前端基础架构搭建 | 1天      | P0     |
| T3-002   | 认证页面开发     | 1天      | P0     |
| T3-003   | 首页开发         | 1天      | P0     |
| T3-004   | 论文模块前端     | 1.5天    | P0     |
| T3-005   | 经验贴模块前端   | 1.5天    | P0     |
| T3-006   | 下载中心模块前端 | 1天      | P1     |
| T3-007   | 个人中心模块前端 | 1天      | P0     |
| T3-008   | 管理员页面前端   | 1天      | P1     |
| T3-009   | 搜索功能前端     | 0.5天    | P0     |
| T3-010   | 公共组件开发     | 1天      | P0     |

---

## 任务详情

### T3-001: 前端基础架构搭建

| 属性               | 内容             |
| ------------------ | ---------------- |
| **任务编号** | T3-001           |
| **任务名称** | 前端基础架构搭建 |
| **预估工时** | 1天              |
| **优先级**   | P0               |

**任务描述**：
搭建前端项目基础架构，配置路由、状态管理、HTTP客户端等核心功能。

**具体步骤**：

1. 配置Vue Router路由
2. 配置Pinia状态管理
3. 配置Axios HTTP客户端
4. 配置Element Plus UI库
5. 创建API接口封装
6. 创建权限控制逻辑
7. 创建全局样式
8. 创建布局组件

**核心代码** (`frontend/src/router/index.ts`)：

```typescript
import { createRouter, createWebHistory } from 'vue-router'
import { useUserStore } from '@/store/user'

const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/Login.vue'),
    meta: { public: true }
  },
  {
    path: '/register',
    name: 'Register',
    component: () => import('@/views/Register.vue'),
    meta: { public: true }
  },
  {
    path: '/',
    component: () => import('@/layouts/MainLayout.vue'),
    children: [
      { path: '', name: 'Home', component: () => import('@/views/Home.vue') },
      { path: 'papers', name: 'Papers', component: () => import('@/views/Papers.vue') },
      { path: 'posts', name: 'Posts', component: () => import('@/views/Posts.vue') },
      { path: 'downloads', name: 'Downloads', component: () => import('@/views/Downloads.vue') },
      { path: 'profile', name: 'Profile', component: () => import('@/views/Profile.vue') },
      { path: 'admin', name: 'Admin', component: () => import('@/views/Admin.vue'), meta: { admin: true } }
    ]
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  
  if (!to.meta.public && !userStore.token) {
    next('/login')
  } else if (to.meta.admin && !userStore.isAdmin) {
    next('/')
  } else {
    next()
  }
})

export default router
```

**依赖关系**：

- 依赖：T1-003 前端基础环境搭建

**验收条件**：

- [ ] 路由配置完成
- [ ] 状态管理配置完成
- [ ] HTTP客户端配置完成
- [ ] 权限控制正常
- [ ] 页面能正常跳转

---

### T3-002: 认证页面开发

| 属性               | 内容         |
| ------------------ | ------------ |
| **任务编号** | T3-002       |
| **任务名称** | 认证页面开发 |
| **预估工时** | 1天          |
| **优先级**   | P0           |

**任务描述**：
开发登录页、注册页，实现用户认证功能。

**具体步骤**：

1. 开发登录页面
2. 开发注册页面
3. 实现表单验证
4. 实现记住密码功能
5. 实现7天免登录功能
6. 集成后端API
7. 处理登录状态

**核心代码** (`frontend/src/views/Login.vue`)：

```vue
<template>
  <div class="login-container">
    <el-card class="login-card">
      <h2>AIForum 登录</h2>
      <el-form :model="form" :rules="rules" ref="formRef">
        <el-form-item prop="account">
          <el-input v-model="form.account" placeholder="学号/姓名" />
        </el-form-item>
        <el-form-item prop="password">
          <el-input v-model="form.password" type="password" placeholder="密码" />
        </el-form-item>
        <el-form-item>
          <el-checkbox v-model="form.remember">记住密码</el-checkbox>
          <el-checkbox v-model="form.autoLogin">7天免登录</el-checkbox>
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="handleLogin" :loading="loading">登录</el-button>
          <el-button @click="$router.push('/register')">注册</el-button>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'
import { useRouter } from 'vue-router'
import { useUserStore } from '@/store/user'
import { login } from '@/api/auth'

const router = useRouter()
const userStore = useUserStore()
const formRef = ref()
const loading = ref(false)

const form = reactive({
  account: '',
  password: '',
  remember: false,
  autoLogin: false
})

const rules = {
  account: [{ required: true, message: '请输入学号或姓名', trigger: 'blur' }],
  password: [{ required: true, message: '请输入密码', trigger: 'blur' }]
}

const handleLogin = async () => {
  await formRef.value.validate()
  loading.value = true
  try {
    const res = await login(form)
    userStore.setToken(res.access_token, form.autoLogin)
    userStore.setUserInfo(res.user)
    router.push('/')
  } finally {
    loading.value = false
  }
}
</script>
```

**依赖关系**：

- 依赖：T3-001 前端基础架构搭建
- 依赖：T2-002 用户认证模块（后端）

**验收条件**：

- [ ] 登录页面正常显示
- [ ] 注册页面正常显示
- [ ] 表单验证正常
- [ ] 登录功能正常
- [ ] 注册功能正常

---

### T3-003: 首页开发

| 属性               | 内容     |
| ------------------ | -------- |
| **任务编号** | T3-003   |
| **任务名称** | 首页开发 |
| **预估工时** | 1天      |
| **优先级**   | P0       |

**任务描述**：
开发首页，包含论文推荐、经验贴推荐、搜索框、个人信息显示。

**具体步骤**：

1. 开发首页布局
2. 开发论文推荐列表组件
3. 开发经验贴推荐列表组件
4. 开发搜索框组件
5. 开发欢迎信息组件
6. 集成后端API
7. 实现响应式布局

**核心代码** (`frontend/src/views/Home.vue`)：

```vue
<template>
  <div class="home-container">
    <!-- 欢迎信息 -->
    <div class="welcome-section">
      <h2>{{ welcomeMessage }}</h2>
      <p>{{ currentDate }}</p>
    </div>
  
    <!-- 搜索框 -->
    <div class="search-section">
      <SearchBox @search="handleSearch" />
    </div>
  
    <!-- 论文推荐 -->
    <div class="section">
      <h3>论文推荐</h3>
      <PaperList :papers="recommendedPapers" />
    </div>
  
    <!-- 经验贴推荐 -->
    <div class="section">
      <h3>经验贴推荐</h3>
      <PostList :posts="recommendedPosts" />
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useUserStore } from '@/store/user'
import { getRecommendedPapers } from '@/api/papers'
import { getRecommendedPosts } from '@/api/posts'
import SearchBox from '@/components/SearchBox.vue'
import PaperList from '@/components/PaperList.vue'
import PostList from '@/components/PostList.vue'

const userStore = useUserStore()
const recommendedPapers = ref([])
const recommendedPosts = ref([])

const welcomeMessage = computed(() => {
  const user = userStore.userInfo
  if (user?.role === 'teacher') {
    return `你好，${user.name}教授，欢迎！`
  }
  return `你好，${user?.name}同学，欢迎！`
})

const currentDate = computed(() => {
  const date = new Date()
  const weekdays = ['星期日', '星期一', '星期二', '星期三', '星期四', '星期五', '星期六']
  return `${date.getFullYear()}年${date.getMonth() + 1}月${date.getDate()}日 ${weekdays[date.getDay()]}`
})

const handleSearch = (keyword: string) => {
  // 跳转到搜索结果页
}

onMounted(async () => {
  recommendedPapers.value = await getRecommendedPapers()
  recommendedPosts.value = await getRecommendedPosts()
})
</script>
```

**依赖关系**：

- 依赖：T3-001 前端基础架构搭建
- 依赖：T2-004 论文管理模块（后端）
- 依赖：T2-005 经验贴模块（后端）

**验收条件**：

- [ ] 首页布局正常
- [ ] 欢迎信息显示正确
- [ ] 论文推荐列表正常
- [ ] 经验贴推荐列表正常
- [ ] 搜索框功能正常

---

### T3-004: 论文模块前端

| 属性               | 内容         |
| ------------------ | ------------ |
| **任务编号** | T3-004       |
| **任务名称** | 论文模块前端 |
| **预估工时** | 1.5天        |
| **优先级**   | P0           |

**任务描述**：
开发论文列表页、详情页、上传页。

**具体步骤**：

1. 开发论文列表页面
2. 开发论文详情页面
3. 开发论文上传页面
4. 实现文件上传组件
5. 实现论文解析进度显示
6. 实现论文信息编辑
7. 实现PDF预览/下载

**依赖关系**：

- 依赖：T3-001 前端基础架构搭建
- 依赖：T2-004 论文管理模块（后端）

**验收条件**：

- [ ] 论文列表页正常
- [ ] 论文详情页正常
- [ ] 论文上传功能正常
- [ ] 文件上传组件正常
- [ ] PDF下载功能正常

---

### T3-005: 经验贴模块前端

| 属性               | 内容           |
| ------------------ | -------------- |
| **任务编号** | T3-005         |
| **任务名称** | 经验贴模块前端 |
| **预估工时** | 1.5天          |
| **优先级**   | P0             |

**任务描述**：
开发经验贴列表页、详情页、发布页，集成富文本编辑器。

**具体步骤**：

1. 开发经验贴列表页面
2. 开发经验贴详情页面
3. 开发经验贴发布页面
4. 集成富文本编辑器（TinyMCE/Quill）
5. 实现图片上传功能
6. 实现附件管理功能
7. 实现Markdown上传转换
8. 实现评论功能

**核心代码** (`frontend/src/components/RichEditor.vue`)：

```vue
<template>
  <div class="rich-editor">
    <Editor
      v-model="content"
      :init="editorInit"
      :disabled="disabled"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, watch } from 'vue'
import Editor from '@tinymce/tinymce-vue'
import { uploadImage } from '@/api/upload'

const props = defineProps<{
  modelValue: string
  disabled?: boolean
}>()

const emit = defineEmits(['update:modelValue'])

const content = ref(props.modelValue)

watch(content, (val) => {
  emit('update:modelValue', val)
})

const editorInit = {
  height: 500,
  menubar: false,
  plugins: [
    'advlist autolink lists link image charmap print preview anchor',
    'searchreplace visualblocks code fullscreen',
    'insertdatetime media table paste code help wordcount'
  ],
  toolbar:
    'undo redo | formatselect | bold italic backcolor | \
    alignleft aligncenter alignright alignjustify | \
    bullist numlist outdent indent | removeformat | help',
  images_upload_handler: async (blobInfo: any) => {
    const file = blobInfo.blob()
    const res = await uploadImage(file)
    return res.url
  }
}
</script>
```

**依赖关系**：

- 依赖：T3-001 前端基础架构搭建
- 依赖：T2-005 经验贴模块（后端）
- 依赖：T2-007 论坛功能模块（后端）

**验收条件**：

- [ ] 经验贴列表页正常
- [ ] 经验贴详情页正常
- [ ] 富文本编辑器正常
- [ ] 图片上传功能正常
- [ ] 评论功能正常

---

### T3-006: 下载中心模块前端

| 属性               | 内容             |
| ------------------ | ---------------- |
| **任务编号** | T3-006           |
| **任务名称** | 下载中心模块前端 |
| **预估工时** | 1天              |
| **优先级**   | P1               |

**任务描述**：
开发下载中心列表页、详情页、上传页（管理员）。

**具体步骤**：

1. 开发下载中心列表页面
2. 开发下载中心详情页面
3. 开发资源上传页面（管理员）
4. 实现文件下载功能
5. 实现下载次数显示

**依赖关系**：

- 依赖：T3-001 前端基础架构搭建
- 依赖：T2-006 下载中心模块（后端）

**验收条件**：

- [ ] 下载中心列表页正常
- [ ] 下载中心详情页正常
- [ ] 资源上传功能正常（管理员）
- [ ] 文件下载功能正常

---

### T3-007: 个人中心模块前端

| 属性               | 内容             |
| ------------------ | ---------------- |
| **任务编号** | T3-007           |
| **任务名称** | 个人中心模块前端 |
| **预估工时** | 1天              |
| **优先级**   | P0               |

**任务描述**：
开发个人中心页面，包含个人信息、我的发布、我的收藏。

**具体步骤**：

1. 开发个人中心布局
2. 开发个人信息页面
3. 开发我的发布页面
4. 开发我的收藏页面
5. 实现密码修改功能
6. 实现信息编辑功能

**依赖关系**：

- 依赖：T3-001 前端基础架构搭建
- 依赖：T2-003 用户管理模块（后端）

**验收条件**：

- [ ] 个人中心布局正常
- [ ] 个人信息显示正常
- [ ] 我的发布列表正常
- [ ] 我的收藏列表正常
- [ ] 密码修改功能正常

---

### T3-008: 管理员页面前端

| 属性               | 内容           |
| ------------------ | -------------- |
| **任务编号** | T3-008         |
| **任务名称** | 管理员页面前端 |
| **预估工时** | 1天            |
| **优先级**   | P1             |

**任务描述**：
开发管理员页面，包含用户管理、内容管理功能。

**具体步骤**：

1. 开发管理员页面布局
2. 开发用户管理页面
3. 开发论文管理页面
4. 开发经验贴管理页面
5. 开发下载中心管理页面
6. 实现权限设置功能

**依赖关系**：

- 依赖：T3-001 前端基础架构搭建
- 依赖：T2-003 用户管理模块（后端）

**验收条件**：

- [ ] 管理员页面布局正常
- [ ] 用户管理功能正常
- [ ] 内容管理功能正常
- [ ] 权限设置功能正常

---

### T3-009: 搜索功能前端

| 属性               | 内容         |
| ------------------ | ------------ |
| **任务编号** | T3-009       |
| **任务名称** | 搜索功能前端 |
| **预估工时** | 0.5天        |
| **优先级**   | P0           |

**任务描述**：
开发搜索功能，包含搜索框、搜索结果页。

**具体步骤**：

1. 开发搜索框组件
2. 开发搜索结果页面
3. 实现搜索结果高亮
4. 实现搜索结果分类
5. 实现搜索历史记录

**依赖关系**：

- 依赖：T3-001 前端基础架构搭建
- 依赖：T2-008 搜索功能模块（后端）

**验收条件**：

- [ ] 搜索框组件正常
- [ ] 搜索结果页正常
- [ ] 搜索结果高亮正常
- [ ] 搜索分类正常

---

### T3-010: 公共组件开发

| 属性               | 内容         |
| ------------------ | ------------ |
| **任务编号** | T3-010       |
| **任务名称** | 公共组件开发 |
| **预估工时** | 1天          |
| **优先级**   | P0           |

**任务描述**：
开发公共组件，包含导航栏、标签栏、页脚等。

**具体步骤**：

1. 开发导航栏组件
2. 开发标签栏组件
3. 开发页脚组件
4. 开发分页组件
5. 开发加载状态组件
6. 开发空状态组件

**核心代码** (`frontend/src/components/NavBar.vue`)：

```vue
<template>
  <el-menu
    :default-active="activeIndex"
    class="nav-bar"
    router
  >
    <el-menu-item index="/">
      <el-icon><HomeFilled /></el-icon>
      <span>首页</span>
    </el-menu-item>
    <el-menu-item index="/papers">
      <el-icon><Document /></el-icon>
      <span>论文</span>
    </el-menu-item>
    <el-menu-item index="/posts">
      <el-icon><ChatDotRound /></el-icon>
      <span>经验贴</span>
    </el-menu-item>
    <el-menu-item index="/downloads">
      <el-icon><Download /></el-icon>
      <span>下载中心</span>
    </el-menu-item>
    <el-menu-item index="/profile">
      <el-icon><User /></el-icon>
      <span>个人中心</span>
    </el-menu-item>
    <el-menu-item v-if="isAdmin" index="/admin">
      <el-icon><Setting /></el-icon>
      <span>管理员</span>
    </el-menu-item>
  </el-menu>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useRoute } from 'vue-router'
import { useUserStore } from '@/store/user'

const route = useRoute()
const userStore = useUserStore()

const activeIndex = computed(() => route.path)
const isAdmin = computed(() => userStore.isAdmin)
</script>
```

**依赖关系**：

- 依赖：T3-001 前端基础架构搭建

**验收条件**：

- [ ] 导航栏组件正常
- [ ] 标签栏组件正常
- [ ] 分页组件正常
- [ ] 其他公共组件正常

---

## 阶段三总结

| 任务                    | 状态   | 工时            |
| ----------------------- | ------ | --------------- |
| T3-001 前端基础架构搭建 | 待开始 | 1天             |
| T3-002 认证页面开发     | 待开始 | 1天             |
| T3-003 首页开发         | 待开始 | 1天             |
| T3-004 论文模块前端     | 待开始 | 1.5天           |
| T3-005 经验贴模块前端   | 待开始 | 1.5天           |
| T3-006 下载中心模块前端 | 待开始 | 1天             |
| T3-007 个人中心模块前端 | 待开始 | 1天             |
| T3-008 管理员页面前端   | 待开始 | 1天             |
| T3-009 搜索功能前端     | 待开始 | 0.5天           |
| T3-010 公共组件开发     | 待开始 | 1天             |
| **总计**          | -      | **9.5天** |

---

**下一步**：进入阶段四：AI功能开发
