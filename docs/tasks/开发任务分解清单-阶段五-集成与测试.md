# AIForum 课题组学术交流平台

## 开发任务分解清单 - 阶段五：集成与测试

| 文档信息 | 内容 |
|----------|------|
| 文档版本 | V1.0 |
| 创建日期 | 2026-03-04 |
| 编写人员 | 技术负责人 |

---

## 任务清单概览

| 任务编号 | 任务名称 | 预估工时 | 优先级 |
|----------|----------|----------|--------|
| T5-001 | 前后端联调 | 1天 | P0 |
| T5-002 | 接口测试 | 1天 | P0 |
| T5-003 | 功能测试 | 1.5天 | P0 |
| T5-004 | 性能测试 | 0.5天 | P1 |
| T5-005 | 安全测试 | 0.5天 | P1 |
| T5-006 | 响应式适配 | 0.5天 | P0 |
| T5-007 | 容器化部署 | 0.5天 | P1 |
| T5-008 | 数据备份配置 | 0.5天 | P1 |
| T5-009 | 用户验收测试 | 1天 | P0 |
| T5-010 | 上线部署 | 0.5天 | P0 |

---

## 任务详情

### T5-001: 前后端联调

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-001 |
| **任务名称** | 前后端联调 |
| **预估工时** | 1天 |
| **优先级** | P0 |

**任务描述**：
完成前后端接口联调，确保数据传输正常。

**具体步骤**：
1. 配置跨域支持
2. 测试所有API接口
3. 验证数据格式
4. 处理接口异常情况
5. 优化接口响应速度
6. 修复联调中发现的问题

**核心代码** (`backend/app/main.py` - CORS配置)：
```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173", "http://localhost:80"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**联调检查清单**：
- [ ] 登录/注册接口
- [ ] 用户信息接口
- [ ] 论文CRUD接口
- [ ] 经验贴CRUD接口
- [ ] 评论/点赞/收藏接口
- [ ] 搜索接口
- [ ] 文件上传/下载接口
- [ ] AI对话接口

**依赖关系**：
- 依赖：T2-009 文件存储模块
- 依赖：T3-010 公共组件开发
- 依赖：T4-007 AI对话前端组件

**验收条件**：
- [ ] 所有API接口联调通过
- [ ] 数据格式正确
- [ ] 异常处理完善
- [ ] 响应速度达标

---

### T5-002: 接口测试

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-002 |
| **任务名称** | 接口测试 |
| **预估工时** | 1天 |
| **优先级** | P0 |

**任务描述**：
编写并执行接口测试用例，确保API功能正确。

**具体步骤**：
1. 配置pytest测试框架
2. 编写认证模块测试用例
3. 编写用户模块测试用例
4. 编写论文模块测试用例
5. 编写经验贴模块测试用例
6. 编写论坛功能测试用例
7. 编写搜索功能测试用例
8. 执行测试并修复问题

**核心代码** (`backend/tests/test_auth.py`)：
```python
import pytest
from fastapi.testclient import TestClient
from app.main import app

client = TestClient(app)

def test_register():
    """测试用户注册"""
    response = client.post("/auth/register", json={
        "name": "测试用户",
        "student_id": "2024001",
        "grade": "2024级",
        "password": "test123456"
    })
    assert response.status_code == 200
    assert response.json()["name"] == "测试用户"

def test_login():
    """测试用户登录"""
    response = client.post("/auth/login", data={
        "username": "2024001",
        "password": "test123456"
    })
    assert response.status_code == 200
    assert "access_token" in response.json()

def test_login_wrong_password():
    """测试错误密码登录"""
    response = client.post("/auth/login", data={
        "username": "2024001",
        "password": "wrongpassword"
    })
    assert response.status_code == 401
```

**测试覆盖范围**：
| 模块 | 测试用例数 | 覆盖率目标 |
|------|------------|------------|
| 认证模块 | 5 | 100% |
| 用户模块 | 8 | 90% |
| 论文模块 | 10 | 90% |
| 经验贴模块 | 8 | 90% |
| 论坛模块 | 6 | 85% |
| 搜索模块 | 4 | 85% |

**依赖关系**：
- 依赖：T5-001 前后端联调

**验收条件**：
- [ ] 测试框架配置完成
- [ ] 所有模块测试用例编写完成
- [ ] 测试通过率 > 90%
- [ ] 代码覆盖率 > 85%

---

### T5-003: 功能测试

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-003 |
| **任务名称** | 功能测试 |
| **预估工时** | 1.5天 |
| **优先级** | P0 |

**任务描述**：
进行完整的功能测试，验证所有功能点正常工作。

**具体步骤**：
1. 制定功能测试计划
2. 编写功能测试用例
3. 执行用户管理功能测试
4. 执行论文管理功能测试
5. 执行经验贴功能测试
6. 执行论坛功能测试
7. 执行搜索功能测试
8. 执行AI功能测试
9. 记录并修复缺陷

**功能测试用例示例**：

| 模块 | 功能点 | 测试步骤 | 预期结果 |
|------|--------|----------|----------|
| 用户管理 | 学生注册 | 1. 进入注册页<br>2. 填写信息<br>3. 点击注册 | 注册成功，跳转到登录页 |
| 用户管理 | 用户登录 | 1. 输入学号<br>2. 输入密码<br>3. 点击登录 | 登录成功，跳转到首页 |
| 论文管理 | 论文上传 | 1. 选择PDF文件<br>2. 点击上传<br>3. 等待解析 | 上传成功，显示解析结果 |
| 论文管理 | 论文下载 | 1. 进入论文详情<br>2. 点击下载按钮 | PDF文件下载成功 |
| 经验贴 | 发布经验贴 | 1. 进入发布页<br>2. 填写内容<br>3. 点击发布 | 发布成功，显示在列表中 |
| 经验贴 | 评论功能 | 1. 进入详情页<br>2. 输入评论<br>3. 点击发送 | 评论成功，显示在评论区 |
| 论坛 | 点赞功能 | 1. 点击点赞按钮 | 点赞数+1，按钮状态改变 |
| 论坛 | 收藏功能 | 1. 点击收藏按钮 | 收藏成功，显示在收藏列表 |
| 搜索 | 论文搜索 | 1. 输入关键词<br>2. 点击搜索 | 显示相关论文列表 |
| AI | AI对话 | 1. 输入问题<br>2. 点击发送 | 显示AI回答和引用 |

**依赖关系**：
- 依赖：T5-001 前后端联调

**验收条件**：
- [ ] 功能测试计划完成
- [ ] 所有功能点测试通过
- [ ] 缺陷修复率 > 95%
- [ ] 无严重缺陷

---

### T5-004: 性能测试

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-004 |
| **任务名称** | 性能测试 |
| **预估工时** | 0.5天 |
| **优先级** | P1 |

**任务描述**：
进行性能测试，确保系统满足性能要求。

**具体步骤**：
1. 制定性能测试指标
2. 配置性能测试工具
3. 执行负载测试
4. 执行压力测试
5. 分析性能瓶颈
6. 优化性能问题

**性能指标**：

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 页面加载时间 | < 2秒 | 首屏加载时间 |
| API响应时间 | < 500ms | 95%的请求 |
| 并发用户数 | 30 | 同时在线用户 |
| 吞吐量 | 100 RPS | 每秒请求数 |
| 数据库查询 | < 100ms | 单条查询 |

**性能测试脚本** (`backend/tests/performance/locustfile.py`)：
```python
from locust import HttpUser, task, between

class WebsiteUser(HttpUser):
    wait_time = between(1, 5)
    
    def on_start(self):
        """登录获取token"""
        response = self.client.post("/auth/login", data={
            "username": "test_user",
            "password": "test_pass"
        })
        self.token = response.json()["access_token"]
    
    @task(3)
    def view_papers(self):
        """查看论文列表"""
        self.client.get("/api/papers", headers={
            "Authorization": f"Bearer {self.token}"
        })
    
    @task(2)
    def view_posts(self):
        """查看经验贴列表"""
        self.client.get("/api/posts", headers={
            "Authorization": f"Bearer {self.token}"
        })
    
    @task(1)
    def search(self):
        """搜索功能"""
        self.client.get("/api/search?keyword=test", headers={
            "Authorization": f"Bearer {self.token}"
        })
```

**依赖关系**：
- 依赖：T5-001 前后端联调

**验收条件**：
- [ ] 性能测试工具配置完成
- [ ] 所有性能指标达标
- [ ] 性能瓶颈已优化
- [ ] 性能测试报告完成

---

### T5-005: 安全测试

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-005 |
| **任务名称** | 安全测试 |
| **预估工时** | 0.5天 |
| **优先级** | P1 |

**任务描述**：
进行安全测试，确保系统安全性。

**具体步骤**：
1. 制定安全测试计划
2. 进行SQL注入测试
3. 进行XSS攻击测试
4. 进行CSRF攻击测试
5. 进行权限绕过测试
6. 进行文件上传安全测试
7. 修复安全漏洞

**安全检查清单**：

| 检查项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| SQL注入 | 输入特殊字符 | 无SQL注入漏洞 |
| XSS攻击 | 输入脚本代码 | 脚本被转义 |
| CSRF攻击 | 伪造请求 | 请求被拒绝 |
| 权限控制 | 访问无权限接口 | 返回403错误 |
| 密码安全 | 检查密码存储 | 密码已加密 |
| JWT安全 | 检查Token验证 | Token验证正确 |
| 文件上传 | 上传恶意文件 | 文件被拒绝 |
| 敏感信息 | 检查日志输出 | 无敏感信息泄露 |

**依赖关系**：
- 依赖：T5-001 前后端联调

**验收条件**：
- [ ] 安全测试计划完成
- [ ] 无高危安全漏洞
- [ ] 中危漏洞已修复
- [ ] 安全测试报告完成

---

### T5-006: 响应式适配

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-006 |
| **任务名称** | 响应式适配 |
| **预估工时** | 0.5天 |
| **优先级** | P0 |

**任务描述**：
实现响应式布局，支持手机和平板访问。

**具体步骤**：
1. 配置响应式断点
2. 适配导航栏（移动端抽屉菜单）
3. 适配内容区域（网格布局）
4. 适配表单（移动端优化）
5. 适配表格（横向滚动）
6. 测试各设备显示效果

**核心代码** (`frontend/src/styles/responsive.scss`)：
```scssn// 响应式断点
$breakpoints: (
  'xs': 0,
  'sm': 576px,
  'md': 768px,
  'lg': 992px,
  'xl': 1200px
);

// 移动端导航栏
@media (max-width: 768px) {
  .nav-bar {
    position: fixed;
    left: -250px;
    width: 250px;
    height: 100vh;
    transition: left 0.3s;
    
    &.open {
      left: 0;
    }
  }
  
  .main-content {
    margin-left: 0;
  }
  
  .mobile-menu-btn {
    display: block;
  }
}

// 响应式网格
.responsive-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
  
  @media (max-width: 1200px) {
    grid-template-columns: repeat(3, 1fr);
  }
  
  @media (max-width: 992px) {
    grid-template-columns: repeat(2, 1fr);
  }
  
  @media (max-width: 576px) {
    grid-template-columns: 1fr;
  }
}
```

**适配设备**：
| 设备类型 | 屏幕宽度 | 布局调整 |
|----------|----------|----------|
| 桌面端 | > 1200px | 完整布局 |
| 平板横屏 | 992-1200px | 侧边栏收缩 |
| 平板竖屏 | 768-992px | 单列布局 |
| 手机 | < 768px | 移动端布局 |

**依赖关系**：
- 依赖：T3-010 公共组件开发

**验收条件**：
- [ ] 响应式断点配置完成
- [ ] 导航栏适配完成
- [ ] 内容区域适配完成
- [ ] 各设备显示正常

---

### T5-007: 容器化部署

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-007 |
| **任务名称** | 容器化部署 |
| **预估工时** | 0.5天 |
| **优先级** | P1 |

**任务描述**：
完成Docker容器化部署配置。

**具体步骤**：
1. 优化Dockerfile
2. 优化docker-compose.yml
3. 配置环境变量
4. 配置数据卷
5. 配置网络
6. 测试容器构建
7. 测试容器运行

**优化后的Dockerfile** (`backend/Dockerfile`)：
```dockerfile
# 多阶段构建
FROM python:3.9-slim as builder

WORKDIR /app

# 安装编译依赖
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# 安装Python依赖
COPY requirements.txt .
RUN pip install --user --no-cache-dir -r requirements.txt

# 运行阶段
FROM python:3.9-slim

WORKDIR /app

# 复制依赖
COPY --from=builder /root/.local /root/.local

# 复制代码
COPY . .

# 设置环境变量
ENV PATH=/root/.local/bin:$PATH
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# 暴露端口
EXPOSE 8000

# 健康检查
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

# 启动命令
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**依赖关系**：
- 依赖：T1-005 Docker容器化配置

**验收条件**：
- [ ] Dockerfile优化完成
- [ ] docker-compose.yml优化完成
- [ ] 容器构建成功
- [ ] 容器运行正常
- [ ] 健康检查正常

---

### T5-008: 数据备份配置

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-008 |
| **任务名称** | 数据备份配置 |
| **预估工时** | 0.5天 |
| **优先级** | P1 |

**任务描述**：
配置每日自动备份机制。

**具体步骤**：
1. 编写数据库备份脚本
2. 编写文件备份脚本
3. 配置定时任务（cron）
4. 配置备份存储
5. 配置备份保留策略
6. 测试备份恢复

**备份脚本** (`scripts/backup.sh`)：
```bash
#!/bin/bash

# 备份配置
BACKUP_DIR="/backup"
DB_NAME="aiforum"
DB_USER="root"
DB_PASS="password"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
echo "开始备份数据库..."
mysqldump -u$DB_USER -p$DB_PASS $DB_NAME > $BACKUP_DIR/db_backup_$DATE.sql

# 压缩备份文件
gzip $BACKUP_DIR/db_backup_$DATE.sql

# 备份上传文件
echo "开始备份文件..."
tar -czf $BACKUP_DIR/uploads_backup_$DATE.tar.gz uploads/

# 删除7天前的备份
echo "清理旧备份..."
find $BACKUP_DIR -name "db_backup_*.sql.gz" -mtime +7 -delete
find $BACKUP_DIR -name "uploads_backup_*.tar.gz" -mtime +7 -delete

echo "备份完成: $DATE"
```

**定时任务配置** (`crontab -e`)：
```bash
# 每天凌晨2点执行备份
0 2 * * * /path/to/scripts/backup.sh >> /var/log/backup.log 2>&1
```

**备份策略**：
| 备份类型 | 频率 | 保留时间 |
|----------|------|----------|
| 数据库全量 | 每日 | 7天 |
| 文件全量 | 每日 | 7天 |

**依赖关系**：
- 依赖：T1-004 数据库环境搭建

**验收条件**：
- [ ] 备份脚本编写完成
- [ ] 定时任务配置完成
- [ ] 备份文件生成正常
- [ ] 备份恢复测试通过

---

### T5-009: 用户验收测试

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-009 |
| **任务名称** | 用户验收测试 |
| **预估工时** | 1天 |
| **优先级** | P0 |

**任务描述**：
进行用户验收测试，确保系统满足业务需求。

**具体步骤**：
1. 制定UAT测试计划
2. 准备测试环境
3. 邀请用户参与测试
4. 执行UAT测试用例
5. 收集用户反馈
6. 修复用户反馈的问题
7. 获取用户验收确认

**UAT测试场景**：

| 场景 | 测试内容 | 验收标准 |
|------|----------|----------|
| 学生注册 | 新学生注册流程 | 注册成功，信息正确 |
| 论文上传 | 上传并解析论文 | 解析正确，信息完整 |
| 经验贴发布 | 发布图文经验贴 | 发布成功，排版正常 |
| 论坛互动 | 评论、点赞、收藏 | 功能正常，数据正确 |
| 搜索功能 | 搜索论文和经验贴 | 结果准确，排序合理 |
| AI对话 | 与AI助手对话 | 回答准确，引用正确 |
| 管理员功能 | 用户管理和内容管理 | 功能完整，权限正确 |

**依赖关系**：
- 依赖：T5-003 功能测试

**验收条件**：
- [ ] UAT测试计划完成
- [ ] 所有测试场景通过
- [ ] 用户反馈已处理
- [ ] 获得用户验收确认

---

### T5-010: 上线部署

| 属性 | 内容 |
|------|------|
| **任务编号** | T5-010 |
| **任务名称** | 上线部署 |
| **预估工时** | 0.5天 |
| **优先级** | P0 |

**任务描述**：
完成系统上线部署。

**具体步骤**：
1. 准备生产环境
2. 配置生产环境变量
3. 构建生产镜像
4. 部署到生产服务器
5. 配置域名和SSL
6. 配置监控和告警
7. 进行上线验证
8. 编写上线文档

**部署检查清单**：

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 服务器准备 | ⬜ | 服务器配置完成 |
| 域名配置 | ⬜ | 域名解析正确 |
| SSL证书 | ⬜ | HTTPS配置完成 |
| 环境变量 | ⬜ | 生产环境变量配置 |
| 数据库初始化 | ⬜ | 数据库表创建完成 |
| 知识库构建 | ⬜ | 向量数据库初始化 |
| 管理员账号 | ⬜ | 初始管理员账号创建 |
| 监控配置 | ⬜ | 监控和告警配置 |
| 备份配置 | ⬜ | 自动备份配置 |
| 上线验证 | ⬜ | 核心功能验证通过 |

**依赖关系**：
- 依赖：T5-009 用户验收测试

**验收条件**：
- [ ] 生产环境部署完成
- [ ] 域名和SSL配置完成
- [ ] 监控和告警配置完成
- [ ] 上线验证通过
- [ ] 上线文档完成

---

## 阶段五总结

| 任务 | 状态 | 工时 |
|------|------|------|
| T5-001 前后端联调 | 待开始 | 1天 |
| T5-002 接口测试 | 待开始 | 1天 |
| T5-003 功能测试 | 待开始 | 1.5天 |
| T5-004 性能测试 | 待开始 | 0.5天 |
| T5-005 安全测试 | 待开始 | 0.5天 |
| T5-006 响应式适配 | 待开始 | 0.5天 |
| T5-007 容器化部署 | 待开始 | 0.5天 |
| T5-008 数据备份配置 | 待开始 | 0.5天 |
| T5-009 用户验收测试 | 待开始 | 1天 |
| T5-010 上线部署 | 待开始 | 0.5天 |
| **总计** | - | **7.5天** |

---

## 项目总体进度

| 阶段 | 任务数 | 预估工时 | 状态 |
|------|--------|----------|------|
| 阶段一：环境搭建 | 6 | 4.5天 | 待开始 |
| 阶段二：后端核心模块 | 9 | 11天 | 待开始 |
| 阶段三：前端核心模块 | 10 | 9.5天 | 待开始 |
| 阶段四：AI功能开发 | 8 | 5.5天 | 待开始 |
| 阶段五：集成与测试 | 10 | 7.5天 | 待开始 |
| **总计** | **43** | **38天** | - |

---

**备注**：
- 以上工时估算基于单人开发，实际开发时间可能因团队规模、经验水平而有所不同
- 建议预留20%的缓冲时间应对突发情况
- 建议采用敏捷开发方式，每2周为一个迭代周期
