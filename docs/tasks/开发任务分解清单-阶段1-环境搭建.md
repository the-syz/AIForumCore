# AIForum 课题组学术交流平台

## 开发任务分解清单 - 阶段1：环境搭建

| 文档信息 | 内容 |
|----------|------|
| 文档版本 | V1.0 |
| 创建日期 | 2026-03-04 |
| 文档状态 | 待执行 |
| 编写人员 | 技术负责人 |

---

## 阶段1：环境搭建（预计工期：3天）

### 1.1 开发环境准备

#### 任务 1.1.1：安装基础开发工具

| 属性 | 内容 |
|------|------|
| **任务标题** | 安装基础开发工具 |
| **任务描述** | 安装项目开发所需的基础环境和工具：<br>1. 安装 Node.js (v16+)<br>2. 安装 Python (v3.9+)<br>3. 安装 Git<br>4. 安装 VS Code 及必要插件<br>5. 安装 MySQL 8.0<br>6. 安装 Redis 6+ |
| **依赖关系** | 无（前置任务） |
| **验收条件** | 1. `node -v` 显示版本 ≥16<br>2. `python --version` 显示版本 ≥3.9<br>3. `git --version` 正常显示<br>4. MySQL服务可正常启动<br>5. Redis服务可正常启动 |
| **预计工时** | 0.5 人/日 |

---

#### 任务 1.1.2：安装Docker及Docker Compose

| 属性 | 内容 |
|------|------|
| **任务标题** | 安装Docker及Docker Compose |
| **任务描述** | 安装容器化部署工具：<br>1. 安装 Docker Desktop（Windows/Mac）或 Docker Engine（Linux）<br>2. 验证 Docker 安装<br>3. 验证 Docker Compose 安装 |
| **依赖关系** | 任务 1.1.1 |
| **验收条件** | 1. `docker --version` 正常显示<br>2. `docker-compose --version` 正常显示<br>3. 可运行 `docker run hello-world` |
| **预计工时** | 0.5 人/日 |

---

### 1.2 项目初始化

#### 任务 1.2.1：创建项目目录结构

| 属性 | 内容 |
|------|------|
| **任务标题** | 创建项目目录结构 |
| **任务描述** | 创建项目基础目录结构：<br>```<br>AIForum/<br>├── backend/          # 后端代码<br>│   ├── app/<br>│   │   ├── api/<br>│   │   ├── core/<br>│   │   ├── models/<br>│   │   ├── schemas/<br>│   │   └── utils/<br>│   ├── tests/<br>│   ├── uploads/<br>│   ├── requirements.txt<br>│   └── main.py<br>├── frontend/         # 前端代码<br>│   ├── src/<br>│   │   ├── api/<br>│   │   ├── components/<br>│   │   ├── views/<br>│   │   ├── router/<br>│   │   ├── store/<br>│   │   └── utils/<br>│   ├── public/<br>│   ├── package.json<br>│   └── vite.config.ts<br>├── docs/             # 文档<br├── docker-compose.yml<br>└── .env<br>``` |
| **依赖关系** | 任务 1.1.2 |
| **验收条件** | 1. 所有目录结构已创建<br>2. 目录权限正确 |
| **预计工时** | 0.5 人/日 |

---

#### 任务 1.2.2：初始化后端项目

| 属性 | 内容 |
|------|------|
| **任务标题** | 初始化后端项目 |
| **任务描述** | 1. 创建 Python 虚拟环境<br>2. 创建 `requirements.txt` 文件，包含以下依赖：<br>   - fastapi<br>   - uvicorn[standard]<br>   - sqlalchemy<br>   - pymysql<br>   - redis<br>   - python-jose[cryptography]<br>   - passlib[bcrypt]<br>   - python-multipart<br>   - pydantic<br>   - pydantic-settings<br>   - pypdf2<br>   - pdfplumber<br>   - python-docx<br>   - requests<br>   - zhipuai<br>   - faiss-cpu<br>   - pytest<br>   - pytest-asyncio<br>3. 安装所有依赖<br>4. 创建 `main.py` 入口文件 |
| **依赖关系** | 任务 1.2.1 |
| **验收条件** | 1. 虚拟环境创建成功<br>2. `pip list` 显示所有依赖已安装<br>3. `python main.py` 可启动（即使报错也是配置问题） |
| **预计工时** | 1 人/日 |

---

#### 任务 1.2.3：初始化前端项目

| 属性 | 内容 |
|------|------|
| **任务标题** | 初始化前端项目 |
| **任务描述** | 1. 使用 Vite 创建 Vue3 + TypeScript 项目：`npm create vite@latest frontend -- --template vue-ts`<br>2. 安装 UI 组件库（Element Plus）：`npm install element-plus`<br>3. 安装其他依赖：<br>   - axios<br>   - vue-router@4<br>   - pinia<br>   - @element-plus/icons-vue<br>   - sass<br>4. 配置 ESLint 和 Prettier<br>5. 验证项目可正常启动 |
| **依赖关系** | 任务 1.2.1 |
| **验收条件** | 1. `npm install` 成功执行<br>2. `npm run dev` 可启动开发服务器<br>3. 浏览器可访问 `http://localhost:5173` |
| **预计工时** | 1 人/日 |

---

### 1.3 数据库初始化

#### 任务 1.3.1：创建数据库和用户

| 属性 | 内容 |
|------|------|
| **任务标题** | 创建数据库和用户 |
| **任务描述** | 1. 登录 MySQL<br>2. 创建数据库：`CREATE DATABASE aiforum CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;`<br>3. 创建用户：`CREATE USER 'aiforum'@'localhost' IDENTIFIED BY 'password';`<br>4. 授权：`GRANT ALL PRIVILEGES ON aiforum.* TO 'aiforum'@'localhost';`<br>5. 刷新权限：`FLUSH PRIVILEGES;` |
| **依赖关系** | 任务 1.1.1 |
| **验收条件** | 1. 数据库 `aiforum` 已创建<br>2. 用户 `aiforum` 可正常登录<br>3. 用户有数据库操作权限 |
| **预计工时** | 0.5 人/日 |

---

#### 任务 1.3.2：配置Redis

| 属性 | 内容 |
|------|------|
| **任务标题** | 配置Redis |
| **任务描述** | 1. 启动 Redis 服务<br>2. 验证 Redis 连接：`redis-cli ping`<br>3. 配置 Redis 密码（可选）<br>4. 测试 Redis 基本操作 |
| **依赖关系** | 任务 1.1.1 |
| **验收条件** | 1. Redis 服务正常运行<br>2. `redis-cli ping` 返回 `PONG`<br>3. 可进行基本的 set/get 操作 |
| **预计工时** | 0.5 人/日 |

---

### 1.4 环境配置

#### 任务 1.4.1：配置环境变量文件

| 属性 | 内容 |
|------|------|
| **任务标题** | 配置环境变量文件 |
| **任务描述** | 创建 `.env` 文件，配置以下内容：<br>```<br># 数据库<br>DATABASE_URL=mysql+pymysql://aiforum:password@localhost:3306/aiforum<br><br># Redis<br>REDIS_URL=redis://localhost:6379/0<br><br># JWT<br>SECRET_KEY=your-super-secret-key-change-in-production<br><br># 智谱API Key (5个)<br>ZHIPU_API_KEY_1=781eec77989041748c33fbe2331b6648.2qeFMlyW7GYFMmwb<br>ZHIPU_API_KEY_2=de48627943ff4c01a0ae221a36e79c46.WYKFZ3uoVsLHYpZp<br>ZHIPU_API_KEY_3=a07c6d5ac89c4eaaa5b70205cf4757d8.O3gQVqSUDuhyToBb<br>ZHIPU_API_KEY_4=d646f59aad144a7ea5b1b84a56974280.r5GkyrHrJyUsnOzT<br>ZHIPU_API_KEY_5=89eebe853f2b46f0b6b96bfa26602426.NE7FXFFSga2nEdHo<br><br># OCR API Key<br>OCR_API_KEY=K85646873688957<br><br># 文件上传<br>FILE_UPLOAD_DIR=./uploads<br>MAX_FILE_SIZE=52428800<br>ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000<br>``` |
| **依赖关系** | 任务 1.3.1, 任务 1.3.2 |
| **验收条件** | 1. `.env` 文件已创建<br>2. 所有环境变量已配置<br>3. 敏感信息未提交到Git |
| **预计工时** | 0.5 人/日 |

---

#### 任务 1.4.2：配置Docker Compose

| 属性 | 内容 |
|------|------|
| **任务标题** | 配置Docker Compose |
| **任务描述** | 创建 `docker-compose.yml` 文件，配置以下服务：<br>1. MySQL 服务<br>2. Redis 服务<br>3. 后端服务<br>4. 前端服务（可选，开发时通常单独启动） |
| **依赖关系** | 任务 1.4.1 |
| **验收条件** | 1. `docker-compose.yml` 文件已创建<br>2. 可执行 `docker-compose config` 验证配置<br>3. 可执行 `docker-compose up -d` 启动服务 |
| **预计工时** | 1 人/日 |

---

### 1.5 Git配置

#### 任务 1.5.1：初始化Git仓库

| 属性 | 内容 |
|------|------|
| **任务标题** | 初始化Git仓库 |
| **任务描述** | 1. 初始化 Git 仓库：`git init`<br>2. 创建 `.gitignore` 文件，排除：<br>   - `__pycache__/`<br>   - `*.pyc`<br>   - `.env`<br>   - `node_modules/`<br>   - `uploads/`<br>   - `.venv/`<br>   - `dist/`<br>   - `*.log`<br>3. 创建初始提交 |
| **依赖关系** | 任务 1.2.2, 任务 1.2.3 |
| **验收条件** | 1. Git 仓库已初始化<br>2. `.gitignore` 文件已创建<br>3. 初始提交已完成 |
| **预计工时** | 0.5 人/日 |

---

## 阶段1任务汇总

| 任务编号 | 任务名称 | 预计工时 | 依赖任务 |
|----------|----------|----------|----------|
| 1.1.1 | 安装基础开发工具 | 0.5 天 | 无 |
| 1.1.2 | 安装Docker及Docker Compose | 0.5 天 | 1.1.1 |
| 1.2.1 | 创建项目目录结构 | 0.5 天 | 1.1.2 |
| 1.2.2 | 初始化后端项目 | 1 天 | 1.2.1 |
| 1.2.3 | 初始化前端项目 | 1 天 | 1.2.1 |
| 1.3.1 | 创建数据库和用户 | 0.5 天 | 1.1.1 |
| 1.3.2 | 配置Redis | 0.5 天 | 1.1.1 |
| 1.4.1 | 配置环境变量文件 | 0.5 天 | 1.3.1, 1.3.2 |
| 1.4.2 | 配置Docker Compose | 1 天 | 1.4.1 |
| 1.5.1 | 初始化Git仓库 | 0.5 天 | 1.2.2, 1.2.3 |
| **总计** | | **6.5 天** | |

**说明**：部分任务可并行执行，实际工期约 **3 天**

---

**阶段1结束，进入阶段2：后端核心模块开发**
